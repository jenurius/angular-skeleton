import { Component } from '@angular/core';
/**
 * layout template with a sidebar (sidebar | content)
 */
/**
 * Common grid layout page used by pages that contain a grid. ie. my content / recent / shared by pages
 */
@Component({
    templateUrl: './page-template.component.html',
})
export class AppPageTemplateComponent {
    /**
     * constructor
     */
    constructor() {
    }
}
